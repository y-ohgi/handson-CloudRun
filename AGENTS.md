# AGENTS.md

AIコーディングエージェント共通の作業規約。特定のツール・モデル・リポジトリに依存しない汎用版として、任意のリポジトリのルートへそのまま置いて使う。

- 適用対象: コード実装、調査、レビュー、運用作業、文書作成のすべて
- ツール固有の設定(skill、subagent、hooks、slash command、MCPなど)は本ファイルに書かず、各ツールの設定ファイルへ置く。本ファイルはツールが変わっても書き換えずに済む内容だけを持つ
- 優先順位: ユーザーの明示的な指示 > 組織・リポジトリ固有の規約 > 本ファイル。矛盾に気づいたら黙って解釈せず、どちらに従うかを述べてから進む

## 基本方針

- 日本語で応答する。コード・変数名・関数名・ファイル名・ブランチ名は英語を使う
- 説明は箇条書きを優先する。テーブルと水平線は、そのほうが明確に読みやすい場合に限って使う
- 既存の設計、命名、フォーマット、ディレクトリ構成を優先する。自分の好みで作り替えない
- 指示された範囲を超える変更をしない。スコープ外の改善点は実装せず、提案として報告する
- 「便利そうだから」で資産(ファイル、スクリプト、抽象)を先回りして増やさない

## 依頼を受けたら最初にすること

手を動かす前に、依頼を次の観点で見立てる。見立て自体は数十秒で終える粒度でよい。

- 規模: 数行の修正か、複数ファイル・設計判断を伴うか
- 曖昧さ: 完了条件(done criteria)が依頼文から一意に決まるか
- リスク: DB、API、認証・認可、課金、権限、削除操作、本番環境に触れるか
- 種類: 実装、調査、レビュー、文書作成、運用作業のどれか(複合か)

見立てに応じて実行戦略を選ぶ。

- 即実行: 小規模・明確・低リスク。完了後に検証して報告する
- 質問先行: 前提が不足していて、もっともらしい解釈を1つも選べない。結果を左右する質問だけに絞る(最大3つ)
- 計画先行: 複数ファイル、設計判断、高リスク領域を含む。計画を提示し承認を得てから着手する
- 調査先行: 判断材料が足りない。調査してから戦略を選び直す
- 分割: 独立した作業が混在している。分割し、依存関係を整理してから独立なものを並列で進める

選んだ戦略と理由を1〜3行で示してから着手する。作業中に見立てが外れたと分かったら、その時点で戦略を切り替えて報告する。

### 計画復唱

即実行を選んだ場合でも、次のいずれかに当てはまるなら、着手前に「1行のプラン + 置いた前提」を提示する。計画書は作らない。

- 完了条件が依頼文から一意に決まらない
- 成果物の形式・対象・順序に複数の選択肢があり、依頼文だけでは絞れない
- 解釈を誤った場合の手戻りが大きい

どちらとも言えない場合は復唱する(1〜2行の提示コストは手戻りより常に小さい)。逆に、対象と完了状態が一意に定まる依頼は復唱せず即着手する。ユーザーの応答を待てない実行形態では、復唱内容を作業結果の報告に含めて進む。

## 事前確認が必要な作業

次に該当する場合は、着手前に対象と方針を提示し、承認を得る。

- 1回の作業で合計20行を超えるコード差分が発生する見込み
- 複数ファイルにまたがる変更、設計判断を伴う変更
- DB、API、認証・認可、課金、権限、個人情報、暗号化に関わる変更
- 大規模なファイル移動、命名変更、責務分割
- 依存パッケージの追加・更新(理由を添えて承認を得る)
- 破壊的な操作(削除、上書き、本番環境への反映、外部への公開・送信)

## 計画の作り方

計画には次を含める。全体で30行以内に収め、これ以上長文化させない。

- 目的: この変更が満たすべき要求(1〜2行)
- done criteria: コマンドや操作で確認できる完了条件。「動くはず」と書かない
- 変更ファイル一覧: 各ファイルで何をするか1行ずつ
- 実装順序: 依存関係を踏まえたステップ
- テスト方針: 追加・更新するテストと、実行するコマンド
- リスクと代替案: 壊れうる箇所、迷った設計判断とその理由

複数案が甲乙つけがたい場合は、推奨案と理由を添えて選択肢を示す。承認前にコードを書き始めない。実装中に計画との乖離(想定外のファイル変更、追加の設計判断)が生じたら、黙って進めず乖離として報告する。

## 委譲と並列化

サブエージェント機能を持つツールでは、メインセッションはオーケストレーターとして振る舞い、実作業を委譲することを既定とする。機能がない環境では、この節の「委譲」を「作業単位を分けて順に片付ける」と読み替える。

必ず委譲する作業:

- 互いに依存しない調査・探索が2つ以上ある(1つのメッセージで同時に起動して並列実行する)
- 答えを出すのに3ファイル以上を読み比べる、または探索範囲が事前に絞れない
- 結論だけが必要で、途中経過のファイル内容をメインの文脈に残す必要がない
- 長い出力(ログ、大量のファイル内容)でメインの文脈を汚染しそうな作業

委譲しない作業:

- 依頼の分解、進捗管理、結果の統合と報告、ユーザーへの確認。これらはメインに残す
- 対象ファイル・シンボル・値が特定済みの1点lookup、直前に自分が読み書きしたファイルの再確認
- 前の作業の文脈に強く依存する判断

委譲時のルール:

- 委譲先へは、目的、期待する成果物の形式、探索範囲、報告してほしい粒度、触ってはいけない範囲の5点を明記する
- 委譲した作業を自分でも重複して行わない。二重作業は時間とコストの浪費
- 委譲先の出力はユーザーには見えない前提で、要点を自分で抽出して伝える
- 委譲は品質のための手段であって目的ではない。単純なタスクを委譲で複雑化しない
- 別モデル・別エージェントへ実装を委譲した場合、失敗(done criteria未達、テスト・lint不通過、エラー終了、同じ不具合の再発)が3回に達したら再委譲を打ち切り、自分で実装する。打ち切りは、推測ではなく観測された失敗理由を列挙して報告する

## 実装

- 変更前に関連ファイルを読み、既存の実装パターン・命名・テスト方式に合わせる
- 仕様が不明確な場合は実装前に確認する。調べれば分かることは質問せず自分で確認する
- 結果を左右しない軽微な不明点は、安全側の仮定を置き、その前提を明示して進める
- 同じアプローチで2回失敗したら、3回目の前に別アプローチを検討する。行き詰まって状況を要約し判断を仰ぐことは失敗ではない
- 最初の仮説に固執しない。反証となる情報を見つけたら、仮説を明示的に更新してから続行する

## 調査

- 調査で答えるべき問いを1〜3個に絞り、「何が分かれば終わりか」を先に決める
- 問いを独立した単位(コードベース探索、公式ドキュメント確認、事例調査)に分解し、独立なものは並列で進める
- 仮説を支持する情報だけでなく、反証を意図的に探す。情報源が矛盾する場合は一次情報(公式ドキュメント、実際のコード、実際の挙動)を優先する
- 可能な限り実地で確かめる。コマンド実行、最小限の再現、テストで検証できるものを推測で終わらせない
- 報告は結論を先頭に置き、根拠(ファイルパス、URL、実行結果)、確度、残る不明点を添える
- 出典の信頼度は、公式ドキュメント・標準仕様・実測(一次)、メンテナ本人の発信(準一次)、実績ある技術ブログや書籍(二次)、個人ブログ・Q&A・SNS(参考、単独では根拠にしない)の順で扱う
- 鮮度が重要な情報(ライブラリのAPI、バージョン、価格、クラウドの仕様)は記憶で断定せず、必ず一次情報を確認する。確認できない場合はその旨を明示する
- 完了条件を満たしたら止める。網羅性を追って際限なく調べ続けない

## レビュー

自分の実装のセルフレビューを含め、レビューは3段階で行う。「ざっと見て」と言われても、省略してよいのは報告粒度だけで、網羅と事実確認は省略しない。

1. coverage-first: 重要度を判断せず、全観点で指摘候補を機械的に集める。「たぶん問題ない」と思っても候補として記録する
2. fact-check: 推測の候補を裏取りし、確認済みか誤検出に倒す。裏取りできないものは「未確認」と明示して残す
3. filter-later: 重要度で分類し、blocker(マージ前に必ず修正) → should(強く推奨) → consider(提案・好み)の順に報告する

確認する観点:

- 正しさ: ロジックの誤り、境界値、null・空・ゼロ件、エラーハンドリングの漏れ、例外の握りつぶし、意図と実装の乖離
- 並行性・冪等性: race condition、リトライ時の二重実行、タイムアウト、ロックの範囲
- セキュリティ: 入力検証、認可の抜け、秘密情報のハードコード・ログ出力、injection、権限の過剰付与
- 信頼性: 失敗時の挙動、フォールバック、graceful shutdown、リソースリーク、依存先障害の伝播
- 可観測性: 障害調査に足りるログ・メトリクス・トレースがあるか、機微情報の混入がないか
- パフォーマンス: N+1、不要なループ・アロケーション、大きな入力でのスケール、キャッシュの妥当性
- 互換性・移行: 破壊的変更、ロールバック可能性、デプロイ順序依存
- テスト: 変更に対応するテストの有無、仕様を検証しているか(実装の写経になっていないか)、境界・異常系
- 可読性・設計: 命名、責務、既存パターンとの一貫性、重複、過剰な抽象化、無関係な変更の混入
- 運用・インフラ: 権限の最小化、コスト影響、クォータ、削除保護、設定値のハードコード

報告のルール:

- 各指摘に「該当箇所(ファイルパス:行)」「事象」「なぜ問題か」「確信度(確認済み / 未確認)」を付け、可能なら修正案を添える
- 差分だけを見て「影響なし」と断定しない。呼び出し元・呼び出し先、設定、マイグレーションを確認する
- 指摘ゼロの場合も、確認した観点の一覧と、何を保証していないかを報告する
- レビューで見つけた問題は指摘に留め、修正は承認後に行う

## 検証と完了報告

- 検証前に完了を報告しない。「実装した」ではなく「実行して確認した」状態で報告する
- 「〜を実装しました」と書いてよいのは、テスト実行、コマンド実行、動作確認のいずれかで裏付けを取った後だけ
- 検証できない場合は「実装したが未検証。理由は〜。リスクは〜」と明示する。「おそらく動く」「〜のはず」を検証済みのように書かない
- 20行超・複数ファイル・done criteriaが明示されたタスクは、完了報告の前に、実装の経緯を知らない文脈で検証する。委譲できる環境なら別エージェントへ「変更の目的」「done criteria」「変更ファイル一覧」だけを渡す(実装理由や自己評価は渡さない)。できない環境では、先入観を捨てて差分を読み直し「仕様を満たすか」「壊れるケースはないか」を自問する
- 完了報告には、実施内容、変更ファイル、テスト結果、未検証項目とそのリスクを含める
- 報告・レビュー・文書では、確認した事実と推測を区別する。確認していない事項は「未確認」「推測」と明示する
- 指摘・主張には具体的な根拠(対象箇所、観測結果、出典)を付ける。裏付けのない断定や重要度の誇張をしない

## 長時間の自律実行と中断耐性

1時間を超える、または3つ以上の区切りに分割できる作業では、中断を「進捗の喪失」ではなく「チェックポイント」に変える。

- 作業を、単独でcommitしてもリポジトリが壊れないmilestoneへ分割する
- 各milestoneに検証方法(テスト、lint、動作確認コマンドのいずれか)を1つ以上割り当てる
- milestoneごとに、検証 → commit → push → 進捗記録の更新を行う
- 進捗記録は作業ブランチのリポジトリルートに置き、次の4点を保つ: Goal(依頼・done criteria・制約)、Milestones(検証方法付きの一覧と完了状態)、Log(完了ごとに実施内容・検証結果・commit hashを1行)、Next(次にやることと再開時の注意)
- 再開時は、進捗記録だけを信じず`git log`と`git status`と突き合わせる。矛盾があればgit状態を正として記録を修正する
- 検証を通過していないmilestoneを完了として記録しない
- 作業完了時(PRを作る場合はPR作成前)に進捗記録を削除し、成果物の差分に混ぜない

人が応答できない時間帯の無人実行では、さらに次を守る。

- 開始前に、必要な操作が実際に許可されているか、認証が通るか、検証コマンドが実行可能かを確かめる。1つでも欠けるなら開始しない
- 人の判断が必要なタスクは実装せず退避し、理由を残す。判断に迷ったら退避側へ倒す
- 権限や安全機構によるブロックを、別表現や別ツールで迂回しない。迂回せず朝へ残す
- 1タスクの失敗で全体を止めず、そのタスクだけ打ち切って残りを続行する。試行上限と全体の打ち切り時刻を先に決めておく
- 終了時に、完了したこと、残したこととその理由、未検証のリスクを1か所にまとめ、再開点が分かる状態にする

## Git

- commitメッセージは英語で、1行目に簡潔な要約、空行を挟んで本文に変更の詳細と理由を書く。Conventional Commits形式を使い、scope(括弧)は付けず `feat: message` の形にする
- 本文には「なぜそうしたか」を書く。差分を見れば分かることを繰り返さない
- commitは必ず機能の粒度ごとに分ける(絶対条件)。責務や目的が異なる変更を1つのcommitに混在させない。迷ったら細かく分ける。細かすぎるcommitは読めるが、混ざったcommitは分解できない
- `git add -A` や `git add .` で作業ツリー全体を一括stageしない。対象を明示する
- commit前に`git diff --staged`でstageした内容と意図が一致しているか確かめる
- 依頼のスコープ外の変更をついでにcommitしない。存在を報告して判断を仰ぐ
- 明示的な指示がない限り、commit、push、branch作成、reset、rebaseを行わない。ただし長時間実行の運用としてcommit/push方針を承認された場合は、その承認を明示的指示とみなす
- `main`(既定ブランチ)へ直接commit・pushしない。作業ブランチを使う
- ブランチ名は`feature/<topic>`のように役割の接頭辞 + 英語kebab-caseとし、Issue番号があれば含める
- PRを作るときはbaseを明示する。既定baseが意図と異なるリポジトリでは特に確認する
- PRのmergeは原則行わない。ユーザーが明示的に許可し、かつCIが通過している場合に限る。レビューが必須のリポジトリでは、許可があってもself-mergeせずレビュアーに委ねる
- 秘密情報を含むファイルをstageしない。変更一覧に現れたらcommitせず、存在だけを報告する

## シェルとコマンド実行

- コード検索は`rg`を優先する。`grep -r`や`find`による全走査を使わない
- 対話型コマンド(`-i`系、確認プロンプトを出すもの)を使わない。非対話フラグで代替できない場合は、コマンドを提示してユーザーへ実行を依頼する
- 破壊的コマンド(`rm -rf`、`git reset --hard`、`git push --force`、`git clean`、ループによる一括削除)は、実行前に対象と影響範囲を提示して承認を得る
- 削除・上書きの前に対象を確認する。見ていないものを消さない
- ビルドやテストなど長時間かかるコマンドはタイムアウトを意識し、必要ならバックグラウンドで実行して進捗を確認する
- コマンドが失敗したら、まず使用しているシェルの設定ファイル(`~/.zshrc`、`~/.bashrc`など)と環境差を疑う
- macOSではBSD系コマンドの挙動(`sed -i ''`など)がGNUと異なり、既定の`/bin/bash`は3.2で連想配列などが使えない点に注意する

## セキュリティ

- `.env`、`.env.*`、秘密鍵、認証トークン、APIキー、証明書、Cookie、セッション情報を含む可能性のあるファイルを参照・表示・変更しない。`.env.example`など秘密情報を含まないテンプレートは対象外
- 秘密情報と思われる値を見つけた場合は、内容を表示せず存在のみを報告する
- ログ、エラーメッセージ、テスト出力に秘密情報や個人情報が含まれる可能性がある場合は、値をマスクして扱う
- 認証・認可・権限・暗号化・決済・個人情報に関わる変更は、実装前に対象範囲と方針を提示して承認を得る
- 複数の顧客・テナント・組織のデータや識別子(リポジトリ名を含む)を扱う場合、混在させない。ある文脈で得た情報を別の文脈へ持ち込まない

## 運用作業

障害対応中(進行中の事象):

- 復旧(mitigate)を根本原因究明より優先する。原因不明でもロールバック等の緩和策を先に提案する
- 仮説は層別に網羅してから証拠でフィルタする(アプリ、データ層、オーケストレーション層、インフラ層、外部要因、そして「観測系そのものの問題」も候補に含める)。単一仮説に固着しない
- 調査はread-onlyに限定する。write系操作(再起動、スケール変更、apply、DBへの書き込み)は提案に留め、承認を得てから行う。実行前に対象環境と影響を復唱する
- 観測した事実と、そこからの解釈を分けて記録・報告する。確認していないコンポーネントを「正常」と報告しない
- タイムライン(時刻、観測事実、実行した操作、判断)と「試したが効果がなかったこと」を随時記録する。事後の振り返りの一次資料になる

事後と平時:

- 振り返り文書はblamelessで書き、タイムラインの事実確認と再発防止策の具体化に集中する
- 手順書は、初見の対応者が深夜でも実行できる粒度で書く。前提条件、判断分岐、戻し方を含める

## 知見の蓄積

- 同じ指摘・修正が2回以上繰り返されたら、規約化して本ファイルまたはツール固有の設定へ反映することを提案する
- 記録は「何をしたか」ではなく「次回どう動くべきか」の形で残す
- 1件1ファイルで残し、1ファイルに複数の教訓を詰め込まない。結論を先に、経緯は後に、10行程度に収める
- 日付は相対表現(「昨日」「先週」)ではなく絶対日付(YYYY-MM-DD)で書く
- 残すもの: 一度失敗して原因が判明したこと、ドキュメント化されていない判断とその理由、再現手順が非自明だったバグの原因、ユーザーから受けた訂正、外部サービスの非自明な挙動
- 残さないもの: リポジトリを読めば自明な事実、一般的な技術知識、秘密情報、セッション終了で無価値になる一時的な状態、原因が確定していない未検証の推測
- 間違っていたと分かった記録は放置せず、その場で更新または削除する

## エージェント資産(prompt、skill、agent定義)を書くとき

- 1資産 = 1責務。「調査もレビューも実装もできる」万能資産を作らない。責務が2つ以上あると気づいたら分割を提案する
- descriptionには「いつ使うか」の具体的なトリガー条件と、「いつ使わないか」の除外条件を1つ以上書く。「便利な」「〜を支援する」のような判断材料にならない抽象表現を使わない
- 指示は検証可能に書く。「丁寧に」「適切に」ではなく、実行・確認できる形にする。数値化できる制約(質問は最大3つ、計画は30行以内)は数値で書く
- 長時間実行・複合タスク向けの資産には、委譲条件、検証条件、進捗の裏付け、曖昧さの扱いの4点を明示的に含める
- 常時読まれる規約は短く保ち、長い手順は必要時に読む別ファイルへ分離する。同じ内容を複数の場所に重複して書かない
- 名前は英語のkebab-caseで、動作を先頭に置く(`review-terraform`、`write-runbook`)。本文は日本語で書く
- 明示的なchain-of-thought要求(「ステップバイステップで考えて」)を書かない。代わりに、出力形式として根拠・判断理由を要求し、done criteriaを制約として与える
- 正常に完了できない場合の挙動(入力不足、曖昧さ、遂行不能)を必ず定義する。失敗を成功として報告させない
- 定義ファイルを書いただけで完成としない。実際のユースケース、特に境界値や大規模データで試走し、設計欠陥がないか確認する。ツールによっては定義の反映にセッション再起動が必要で、その場合は反映後に検証する

## このリポジトリ固有の規約

ここから下は、honkit製のCloud Runハンズオン教材であるこのリポジトリ固有の事項を扱う。

### 構成

- 本文は章ディレクトリ(`00_preparation`〜`99_cleanup`)の`README.md`に置く。発展編のみ`10_advanced/`配下に個別ファイルを持つ
- `SUMMARY.md`が目次の唯一の正。章の追加・削除・改名・並べ替えを行ったら必ず同時に更新する
- `tools/build-pdf.js`は`SUMMARY.md`をパースして章順とPDF内リンクを決めるため、`SUMMARY.md`に載っていないMarkdownはPDFに含まれない
- `code/`は受講者がコピーして使うサンプルアプリ。変更したら参照元の章の本文と手順を同期させる
- `support/`は当日運用のサポートアプリ。`.bookignore`によりGitBook本体のビルド対象外で、教材本体はこれに依存しない

### 検証

- 表示確認は`npm run serve`、ビルド確認は`npm run build`
- `handson-cloudrun.pdf`は`development`/`main`へのmerge時にGitHub Actions
  (`.github/workflows/pdf.yml`)が再生成してcommitする。PRには含めない
- ローカルでの見た目確認に`npm run pdf`を使ってよい(PlaywrightのChromiumが
  必要)。ただし生成物はcommitしない
- 手順やコマンドを追加・変更する場合は、実行して通ることを確認してから書く。確認できない場合はその旨を本文ではなくPRに明示する
